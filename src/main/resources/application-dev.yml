spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.usermanagement: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
